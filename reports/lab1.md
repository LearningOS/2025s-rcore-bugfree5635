# 功能实现

1. 请求参数为0，返回id地址处的值。
2. 请求参数为1，将data填入id地址。
3. 请求参数为2，统计当前任务syscall调用类型为id的次数。

## 统计次数

使用一个数组记录每种syscall的调用次数，在syscall的时候记录，在sys_trace调用时获取调用次数。将该数组保存在当前任务的控制块里。

**数据结构**

使用一个大小等同于syscall种类的数组，数组的每个元素拥有两个字段：

1. syscall：表示系统调用的类型
2. call_times：表示该系统调用的实际调用次数